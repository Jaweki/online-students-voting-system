export type FunctionalityData = {
    title: string,
    desc: string,
    icon: string,
}

export type InputsObject = {
    name: string,
    email: string,
    regNo: string,
    avatar: string,
    password: string,
}

export type RegistrationStatusObject = {
    type: string,
    email: string,
    error: string,
}